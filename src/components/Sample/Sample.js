import React from 'react';
import Button from 'react-bootstrap/Button';

export const Sample = ({ className }) => (
  <>
    <Button variant='primary' calssName={className}>
      Sample Bootstrap-Button
    </Button>
  </>
);
